IF (SELECT `COMPLAINT_STATE`
    FROM `COMPLAINT`
    WHERE `COMPLAINT_ID` = ?) = "REGISTERED"
THEN
    UPDATE `COMPLAINT`
    SET `COMPLAINT_STATE` = "CANCELED",
        `DECISION_DATE` = CURRENT_DATE()
    WHERE `COMPLAINT_ID` = ?;
END IF;